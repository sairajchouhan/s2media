version: '3.9'
services:
  api:
    container_name: s2media_api_dev
    build:
      context: ./apps/server
      dockerfile: ./Dockerfile.dev
    ports:
      - 5000:5000
    depends_on:
      - db
      - redis
    env_file:
      - ./apps/server/.env
    volumes:
      - ./apps/server:/app
      - /app/node_modules
    environment:
      - DATABASE_URL=postgresql://postgres:password@db:5432/s2media?schema=public&connection_limit=5&pool_timeout=0
      - REDIS_HOST=redis

  db:
    image: postgres:latest
    container_name: s2media_postgres_dev
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=s2media
    volumes:
      - postgres-data:/var/lib/postgresql/data/

  redis:
    image: redis:latest
    ports:
      - 6379:6379
    container_name: s2media_redis_dev
    volumes:
      - redis-data:/data

  back:
    container_name: s2media_back_dev
    build:
      context: ./apps/back
      dockerfile: ./Dockerfile.dev
    ports:
      - 8080:8080
    volumes:
      - ./apps/back:/app
      - /app/node_modules
    environment:
      - REDIS_HOST=redis

volumes:
  postgres-data:
  redis-data:
